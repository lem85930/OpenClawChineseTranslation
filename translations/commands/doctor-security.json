{
  "file": "src/commands/doctor-security.ts",
  "description": "安全审计警告",
  "replacements": {
    "CRITICAL: Gateway bound to": "严重警告: 网关绑定到",
    "without authentication.": "但未设置认证。",
    "Anyone on your network (or internet if port-forwarded) can fully control your agent.": "你网络上的任何人（如果做了端口转发则是互联网上的任何人）都可以完全控制你的 AI 助手。",
    "WARNING: Gateway bound to": "警告: 网关绑定到",
    "(network-accessible).": "(可被网络访问)。",
    "Ensure your auth credentials are strong and not exposed.": "请确保你的认证凭据足够强且未被泄露。",
    "Anyone can DM it.": "任何人都可以向它发私信。",
    "unknown senders will be blocked": "未知发送者将被阻止",
    "No channel security warnings detected.": "未检测到频道安全警告。",
    "note(lines.join(\"\\n\"), \"Security\")": "note(lines.join(\"\\n\"), \"安全\")",
    "\"- Note: approvals.exec.enabled=false disables approval forwarding only.\"": "\"- 注意: approvals.exec.enabled=false 仅禁用审批转发。\"",
    "`  Host exec gating still comes from ${resolveExecApprovalsDisplayPath()}.`": "`  主机执行控制仍来自 ${resolveExecApprovalsDisplayPath()}。`",
    "`  Check local policy with: ${formatCliCommand(\"openclaw approvals get --gateway\")}`": "`  使用以下命令检查本地策略: ${formatCliCommand(\"openclaw approvals get --gateway\")}`",
    "\"  Safer remote access: keep bind loopback and use Tailscale Serve/Funnel or an SSH tunnel.\"": "\"  更安全的远程访问方式: 保持绑定回环地址，使用 Tailscale Serve/Funnel 或 SSH 隧道。\"",
    "\"  Example tunnel: ssh -N -L 18789:127.0.0.1:18789 user@gateway-host\"": "\"  隧道示例: ssh -N -L 18789:127.0.0.1:18789 user@gateway-host\"",
    "\"  Docs: https://docs.openclaw.ai/gateway/remote\"": "\"  文档: https://docs.openclaw.ai/gateway/remote\"",
    "`  Fix: ${formatCliCommand(\"openclaw configure\")} to set a password`": "`  修复: ${formatCliCommand(\"openclaw configure\")} 设置密码`",
    "`  Or switch to token: ${formatCliCommand(\"openclaw config set gateway.auth.mode token\")}`": "`  或切换到 token: ${formatCliCommand(\"openclaw config set gateway.auth.mode token\")}`",
    "`  Fix: ${formatCliCommand(\"openclaw doctor --fix\")} to generate a token`": "`  修复: ${formatCliCommand(\"openclaw doctor --fix\")} 生成 token`",
    "`  Or set token directly: ${formatCliCommand(\n                \"openclaw config set gateway.auth.mode token\",\n              )}`": "`  或直接设置 token: ${formatCliCommand(\n                \"openclaw config set gateway.auth.mode token\",\n              )}`",
    "`- Run: ${formatCliCommand(\"openclaw security audit --deep\")}`": "`- 运行: ${formatCliCommand(\"openclaw security audit --deep\")}`"
  }
}
